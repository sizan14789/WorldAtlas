import Helper from "./helper";

export const metadata = {
  title: "Country",
  description: "Country page of World Atlas"
}

const Countries = () => {
  return <Helper />
};

export default Countries;
